version: 2

models: 
  - name: stg_payments
    description: One unique payment record per row
    columns:
      - name: payment_id
        description: Primary key for stg_payments
        tests:
          - unique
          - not_null